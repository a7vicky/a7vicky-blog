import AuthorCard from '../../components/AuthorCard';

export const metadata = {
  title: 'About - @a7vicky blog',
};

export default function AboutPage() {
  return (
    <div className="container">
      <AuthorCard />
    </div>
  );
}
